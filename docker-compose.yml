services:
  node:
    image: node:21.5-bullseye-slim
    container_name: e-motion-web
    working_dir: /application
    volumes:
      - .:/application
    restart: unless-stopped
    tty: true
    ports:
      - ${DOCKER_APP_HOST_PORT:-3000}:80
    command: ["npm", "run", "dev"]
